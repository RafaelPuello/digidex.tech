[pytest]
DJANGO_SETTINGS_MODULE = digidex.settings.testing
python_files = tests.py test_*.py *_tests.py
addopts = --tb=short -p no:warnings
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
